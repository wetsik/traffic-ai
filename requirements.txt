# Slim cloud deployment (Streamlit Community Cloud).
# BI dashboard + Groq chat only — no torch / live detector.
# For the full local app (live YOLO detector) use requirements-local.txt instead.
streamlit==1.56.0
plotly==5.24.1
groq==1.4.0
opencv-python-headless==4.13.0.92
